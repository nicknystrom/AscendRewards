<viewdata model="TemplateCreateModel" />

# using (Html.BeginForm(MVC.Admin.Messaging.Index()))
# {

    ${Html.Scaffold(s => {
            s.Open();
            s.Field(t => t.Name);
            s.Submit("Create Template");
            s.Close();
    })}
    
# }