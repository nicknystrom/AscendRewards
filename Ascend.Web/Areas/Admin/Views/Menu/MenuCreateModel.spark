<viewdata model="MenuCreateModel" />

# using (Html.BeginForm(MVC.Admin.Menu.Index()))
# {

    ${Html.Scaffold(s => {
            s.Open();
            s.Field(m => m.Name);
            s.Submit("Create Menu");
            s.Close();
    })}
    
# }