<viewdata model="TenantCreateModel" />

# using (Html.BeginForm(Url.For<TenantController>(c => c.Index())))
# {
    ${Html.Scaffold(s => {
            s.Open();
            s.Field(t => t.Name);
            s.Submit("Create Tenant");
            s.Close();
    })}    
# }