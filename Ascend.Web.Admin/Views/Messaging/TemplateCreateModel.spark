<viewdata model="TemplateCreateModel" />

# using (Html.BeginForm(Url.For<MessagingController>(c => c.Index())))
# {

    ${Html.Scaffold(s => {
            s.Open();
            s.Field(t => t.Name);
            s.Submit("Create Template");
            s.Close();
    })}
    
# }