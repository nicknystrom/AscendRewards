<viewdata model="CatalogCreateModel" />

# using (Html.BeginForm(Url.For<CatalogController>(c => c.Index())))
# {

    ${Html.Scaffold(s => {
            s.Open();
            s.Field(c => c.Name);
            s.Submit("Create Catalog");
            s.Close();
    })}
    
# }