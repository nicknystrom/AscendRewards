<viewdata model="MenuCreateModel" />

# using (Html.BeginForm(Url.For<MenuController>(c => c.Index())))
# {

    ${Html.Scaffold(s => {
            s.Open();
            s.Field(m => m.Name);
            s.Submit("Create Menu");
            s.Close();
    })}
    
# }